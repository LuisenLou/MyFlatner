import { connect } from "react-redux";

function Navbar(){
    return (
        <nav>
            NavBar
        </nav>
    )
}

const mapStateToProps = state => ({

})

export default connect(mapStateToProps,{
}) (Navbar);